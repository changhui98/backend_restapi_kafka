networks:
  ecommerce:
    external: true

services:
  user-db:
    image: mysql:8
    container_name: user_db
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: user_db
    ports:
      - "3306:3306"
    networks:
      - ecommerce
